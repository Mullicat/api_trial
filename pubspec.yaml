name: api_trial
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2 # For API calls
  flutter_dotenv: ^5.1.0 # For .env API key
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  cached_network_image: ^3.3.1
  supabase_flutter: ^2.9.1
  image_picker: ^1.2.0
  permission_handler: ^11.1.1
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.12
  freezed: ^2.4.4
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env
